version: "3.9"  # optional since v1.27.0

services:
    python-tests:
        build:
            context: ../..
            dockerfile: tests/automation/Dockerfile
        volumes:
            - ../..:/code
        working_dir: /code
        command: ./tests/automation/run_tests.sh
